@page
@using Constants;
@model SwimResults.Pages.EditIntervalModel

@{
    ViewData["Title"] = "Edit Interval";
}

<h3>Edit workout interval @Model.WorkoutInterval.IntervalNo</h3>

<hr />
<div class="row">
    <div class="col-6">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
            <label asp-for="WorkoutInterval.WorkoutId" class="control-label"></label>
            <select asp-for="WorkoutInterval.WorkoutId" class="form-control" asp-items="ViewBag.WorkoutId"></select>
            <span asp-validation-for="WorkoutInterval.WorkoutId" class="text-danger"></span>
        </div>*@
            @*<div class="form-group">
            <label asp-for="WorkoutInterval.TimeOffset" class="control-label"></label>
            <input asp-for="WorkoutInterval.TimeOffset" class="form-control" />
            <span asp-validation-for="WorkoutInterval.TimeOffset" class="text-danger"></span>
        </div>*@

            <input type="hidden" asp-for="WorkoutInterval.Id" />
            <input type="hidden" asp-for="WorkoutInterval.WorkoutId" />
            <input type="hidden" asp-for="WorkoutInterval.IntervalNo" />

            <div class="form-group">
                <label asp-for="WorkoutInterval.WorkoutIntervalTypeId" class="control-label"></label>
                <select asp-for="WorkoutInterval.WorkoutIntervalTypeId" class="form-control" asp-items="Model.WorkoutIntervalTypeSelectList">
                    <option value="">-</option>
                </select>
                <span asp-validation-for="WorkoutInterval.WorkoutIntervalTypeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkoutInterval.Notes" class="control-label"></label>
                <textarea asp-for="WorkoutInterval.Notes" class="form-control"></textarea>
                <span asp-validation-for="WorkoutInterval.Notes" class="text-danger"></span>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-outline-primary"><i class="far fa-save mr-1"></i>Save</button>
                <a href="@TempData.Peek(ValueKeys.TempDataReturnPathKey)" class="btn btn-outline-primary ml-3">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
